127.0.0.1 localhost
<%= node['ipaddress'] %> <%= node['fqdn'].downcase %> <%= node['hostname'].downcase %> <%= node['hosts']['aliases'].collect{|a|a.downcase}.join(' ') unless node['hosts']['aliases'].empty? %>

<% node['hosts']['entries'].each do |ip, name| -%>
<%= ip %> <%= name.downcase %>
<% end -%>
